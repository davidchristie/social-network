import { itRendersWithoutCrashing } from "test-utilities/dist/react";
import ProfileFollowing from ".";

describe("ProfileFollowing component", () => {
  itRendersWithoutCrashing(ProfileFollowing);
});
