{
  "devDependencies": {
    "react-test-renderer": "16.7.0"
  },
  "name": "test-utilities",
  "private": true,
  "scripts": {
    "build": "rimraf dist && tsc",
    "lint": "tslint --project . && tsc --noEmit"
  },
  "version": "1.0.0"
}
